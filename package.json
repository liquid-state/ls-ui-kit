{
  "name": "ui-kit-root",
  "version": "0.4.0",
  "description": "Liquid State UI Kit for Desktop and Mobile.",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "storybook": "lerna run build --scope @liquid-state/ui-kit && start-storybook -p 9002 -c packages/stories/.storybook",
    "lint": "eslint packages/*/src/**/*.{js,jsx}",
    "lint:fix": "eslint packages/*/src/**/*.{js,jsx} --fix"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "1.2.0",
    "lerna": "^3.4.3",
    "lint-staged": "^8.1.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "yarn audit"
    }
  },
  "lint-staged": {
    "src/**/*.{js,jsx}": [
      "eslint --fix",
      "git add"
    ]
  }
}
